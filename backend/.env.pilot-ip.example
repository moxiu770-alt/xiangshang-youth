# Temporary pilot-only endpoint while the mainland domain is being filed.
PUBLIC_HOST=106.52.164.21
ACME_EMAIL=ops@example.com

# Copy the remaining secrets from .env.pilot.example and replace every value.
POSTGRES_DB=xiangshang_youth
POSTGRES_USER=xiangshang
POSTGRES_PASSWORD=replace-with-a-long-random-password
METRICS_TOKEN=replace-with-a-long-random-token
FIELD_DEVICE_SIGNING_ENCRYPTION_KEY=replace-with-32-byte-base64-key
PUSH_TOKEN_ENCRYPTION_KEY=replace-with-at-least-32-random-characters
ALLOW_PUBLIC_REGISTRATION=false
